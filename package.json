{
  "name": "enamels-platform",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd backend && pip install -r requirements.txt && cd ../dashboard && npm install && cd ../customer-app && npm install",
    "backend": "cd EnamelsManufacturing/manufacturing-backend && python -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload",
    "frontend": "cd EnamelsManufacturing/manufacturing && npm run dev",
    "start": "npx -y concurrently \"npm run backend\" \"npm run frontend\" --names \"BACK-POSTGRES,FRONT-REACT\" --prefix-colors \"cyan,yellow\""
  }
}
